# HARNESS — Build Specification

> **Read this whole file before writing code.** It is a self-contained handoff spec. You have no
> prior context; everything you need is here. Every technical claim below was verified against live
> sources on 2026-08-08. Items marked ⚠️ are unverified — confirm before depending on them.

---

## 0. Build status — AS BUILT (updated 2026-08-08, post-integration)

The build is **complete and verified end to end**. This section records what actually shipped and
which claims below turned out to be wrong. Sections marked **✅ AS BUILT** have been corrected in
place; the surrounding prose is left as originally written so the reasoning is still readable.

| Gate | Evidence |
|---|---|
| `forge test` | **14/14 pass** against real mainnet Aave — 2 suites, 0 failed |
| `forge build` | Compiler run successful, exit 0 (solc 0.8.27, evm `cancun`) |
| `next build` | exit 0, TypeScript clean |
| `tsc` (harness-api) | exit 0 |
| Audit engine | flips a Critical on user-edited code, both presets |
| Attack tests emitted | **7 per preset** |
| HTTP routes | 15/15 including CORS preflight |
| Chain scenarios | 4/4 on a Tenderly Virtual Environment |
| Integration | `npm run verify:generated` → *"Agent A output verified end to end"* |

**The demo is VAULT-first.** The ERC-4626 vault preset became the headline (the aToken donation
attack is one deleted function and needs no protocol interaction); the flash-loan receiver is fully
built and still demoable. This inverts the priorities in §7 and §10 — see those sections.

**Live Tenderly VE:** forked at block **25710954**, chain id 1, explorer enabled with verified
source. The fork block is pinned on both sides via `TENDERLY_FORK_BLOCK`.

**Four corrections to this spec**, each found by running the thing rather than reading it:

1. §9.2's `explorer_config` field name/value is wrong — see §9.2.
2. Aave v3.3 uses **custom errors**, not string codes, and rejects dust supplies — see §9.1.
3. The frozen `Scenario` union and `GeneratedProject` both gained a field — see §6.
4. The attack-snippet fixture schema is `harness-attack-snippets/v1`, not the shape §8.2 implies.

**Outstanding:** deploy both apps, and record the backup video (§13).

---

## 1. What we are building

**HARNESS is "OpenZeppelin Wizard, but for DeFi."**

OpenZeppelin's Contracts Wizard (wizard.openzeppelin.com) lets a developer tick checkboxes and get
production-shaped Solidity for tokens, NFTs and governance. It has **no DeFi support at all**.
Meanwhile, developers building contracts that integrate with lending protocols (Aave, Morpho,
Compound) repeatedly ship the same well-documented Critical bugs.

HARNESS has **three pillars**:

| # | Pillar | What the user does | What they get |
|---|---|---|---|
| 1 | **Generate** | Ticks checkboxes for an Aave v3 integration | A hardened Solidity contract + a Foundry project |
| 2 | **Audit** | Optionally edits the generated code, or pastes their own | Findings mapped to real historical hacks, each citing a runnable exploit PoC |
| 3 | **Simulate** | Clicks "run scenario" | The contract deployed to a fork of **real mainnet** with **real Aave**, executing a real supply/borrow/flash-loan, with a call trace |

**Scope for v1: Aave v3 only.** Morpho/Compound are roadmap, not build targets.

**One-line pitch:** *Every wizard generates code. HARNESS generates the code, the attacks on it, and
the proof it survives them.*

---

## 2. Why this is worth building (verified research)

### 2.1 The gap is real
- OZ Wizard's Solidity kinds are exactly: `erc20`, `erc721`, `erc1155`, `stablecoin`,
  `realWorldAsset`, `account`, `governor`, `custom`. **No ERC-4626. No vault. No DeFi.**
- ⭐ OZ's **Stellar** wizard *does* ship a `vault` kind. The Solidity one does not. Best single
  citation that the gap is known and unfilled.
- DuckDuckGo returns literally *"No results found"* for `"aave starter kit" solidity template`.
  No Scaffold-ETH Aave extension exists.

### 2.2 The adjacent markets are already closed — do NOT compete there
- **Aave Kit** (`npm i @aave/react@next`) ships `useSupply()`, `useBorrow()`, `useVaultDeposit()`
  across 14 chains.
- **Morpho SDK** builds slippage-protected viem transactions with `getRequirements()`.
- ⇒ **Never generate frontend/SDK/TypeScript integration code.** That market is owned.
- ⇒ **The on-chain Solidity integration contract is generated by nobody** — and that is precisely
  where every Critical-severity finding in the public audit corpus lives.

### 2.3 The differentiator, in one sentence
Every audit source converges on the same remedy: **fork tests with hostile inputs.** Verbatim from
research: *"That checklist is mechanical and nobody ships it."* Mechanical means generatable.

### 2.4 Naming
⚠️ **"DeFi Wizard" is taken** by an existing startup (ERC20/staking codegen). Do not use it.
Ship as **HARNESS** — you *harness* a protocol, and a *test harness* proves it is safe.

---

## 3. The knowledge base: `sanbir/evm-hack-registry`

<https://github.com/sanbir/evm-hack-registry> — this powers Pillar 2 and is our biggest unfair
advantage. It is a derivative of `SunWeb3Sec/DeFiHackLabs`, but made actually runnable.

### 3.1 What it contains (verified)
- **~845 exploit PoC folders**, `YYYY-MM-Name_exp/`, 2017 → 2026. ~811 have
  `Suite result: ok` in their committed `output.txt`.
- Each folder is a **standalone Foundry project** with its own `foundry.toml` and correct solc
  version (no cross-version conflicts).
- Each contains:

| Artifact | What it is |
|---|---|
| `src/`, `test/` | the exploit harness |
| `sources/` | the **actual victim contract source**, pulled from Etherscan |
| `anvil_state.json` | captured on-chain block state — **runs fully offline** |
| `output.txt` | reference `forge test` trace |
| `<Name>_exp.md` | AI-written root-cause + attack-flow write-up |

- **No RPC keys, no archive node, no internet needed at run time.**
  `_shared/run-poc/run_poc.sh 2018-04-BEC_exp -vvvvv` spins a private anvil on an OS-assigned port
  loaded from the snapshot. `run_all.sh` runs everything in parallel.
- Docker: `docker build -t evm-hack-registry .` then
  `docker run --rm --network none evm-hack-registry 2018-04-BEC_exp`.

### 3.2 The vulnerability taxonomy — this is the part we actually use
Every PoC is tagged with classes from the [AuditVault](https://github.com/AuditWare/AuditVault)
taxonomy, written as a single line under each write-up's title:

> **Vulnerability classes:** vuln/oracle/price-manipulation · vuln/governance/flash-loan-attack

~820 write-ups are tagged; **1,851+ tag instances** across **70 distinct `vuln/` slugs**
(avg ~2.3 per PoC). Grep-able:

```bash
grep -rl "vuln/oracle/price-manipulation" --include='*_exp.md' .
```

**By category:**

| Category | Tags | Covers |
|---|---:|---|
| `vuln/logic/…` | 538 | state updates, ordering, price/reward/fee/liquidation math, missing checks |
| `vuln/access-control/…` | 425 | missing auth/modifier/owner-check, uninitialized proxy, centralization |
| `vuln/oracle/…` | 327 | price manipulation, spot-price, stale-price, single-source, TWAP, wrong feed |
| `vuln/defi/…` | 166 | AMM slippage, sandwich, fee manipulation |
| `vuln/arithmetic/…` | 112 | overflow/underflow, rounding, precision loss, decimal mismatch |
| `vuln/dependency/…` | 90 | unsafe external calls, unchecked return values, proxy hazards |
| `vuln/governance/…` | 65 | flash-loan voting, proposal manipulation, timelock bypass |
| `vuln/reentrancy/…` | 63 | single/cross-function/cross-contract/**read-only** |
| `vuln/auth/…` | 18 | signature replay/malleability/validation |
| `vuln/bridge/…` | 16 | message spoofing, missing validation, replay |
| `vuln/input-validation/…` | 15 | missing/boundary/wrong-type checks |
| `vuln/data/…`, `vuln/dos/…` | 7 each | uninitialized storage; frozen funds, griefing, unbounded loops |

**Top individual classes:** `access-control/missing-auth` 254 · `oracle/price-manipulation` 176 ·
`defi/slippage` 120 · `oracle/spot-price` 113 · `logic/state-update` 94 ·
`logic/incorrect-state-transition` 87 · `dependency/unsafe-external-call` 74 ·
`logic/missing-check` 71 · `logic/incorrect-order-of-operations` 71 · `logic/missing-validation` 68 ·
`arithmetic/rounding` 35 · `arithmetic/precision-loss` 32 · `oracle/stale-price` 14.

### 3.3 ⚠️ CRITICAL OPERATIONAL WARNING
**Do NOT `git clone` the whole registry during the hackathon.** ~845 folders each carrying an
`anvil_state.json` snapshot — this is very large and hackathon wifi will kill you.

**We do not need to run their PoCs.** We need their *taxonomy and citations*. Do this instead:

1. **Sparse-checkout or manually copy ~15 relevant `_exp.md` write-ups only** (see the shortlist in
   §5.3). Text files only, no `anvil_state.json`.
2. Hand-curate those into a single committed `knowledge/findings.json` in our repo.
3. Link out to the GitHub folder URL for "run this exploit yourself."

If even that is too slow, **the shortlist in §5.3 already contains everything you need** — the
registry link becomes a citation, not a dependency.

⚠️ **License is ambiguous**: the README says *"Same as the source — see the individual PoCs and
DeFiHackLabs."* Treat as: cite and link freely, **do not vendor their Solidity into our repo**.
Attribute both `sanbir/evm-hack-registry` and `SunWeb3Sec/DeFiHackLabs` in our README.

### 3.4 Aave / lending / vault PoCs spotted in the tree (useful citations)
`2024-08-AAVE_Repay_Adapter` · `2025-06-AAVEBoost_exp` ·
`2026-04-AaveRebalancerCreditDelegation_exp` · `2021-03-dodo_flashloan_exp` ·
`2020-10-HarvestFinance_exp` · `2024-06-UwuLend_First_exp` / `_Second_exp` ·
`2021-08-Cream_exp` · `2021-10-Cream_2_exp` · `2023-03-Euler_exp` · `2024-10-MorphoBlue_exp` ·
`2026-06-Vault4626_exp` · `2026-04-ThetanutsVaultShareRounding_exp` · `2026-02-Moonwell_exp` ·
`2025-11-Moonwell_exp` · `2024-11-PolterFinance_exploit` · `2025-08-SizeFlashLoanLooping_exp` ·
`27529-h-39-aavestrategysol-changing-swapper-breaks-the-contract-co_exp` ·
`27531-h-41-rewards-compounded-in-aavestrategy-are-unredeemable-cod_exp` ·
`25134-h-05-routers-are-not-enforced-to-repay-aave-portal-loan-code_exp`

---

## 4. Constraints

- **14 hours. 2 AI agents working in parallel.** Wall-clock, not ideal-hours.
- **Paid Tenderly credits available.** Nothing else is paid.
- Foundry + WSL installed locally.
- **Judging:** Technical 50% / Practical 25% / Innovation 25%. A live working demo is weighted
  heavily.
- Generation must be **deterministic template composition, NOT AI**. Determinism is the feature —
  it is why people trust OZ Wizard's output, and it means our demo cannot fail to compile.

---

## 5. Architecture

### 5.1 Repo layout — TWO INDEPENDENT APPS, ONE GIT REPO  ✅ AS BUILT

This structure exists specifically so the two agents never touch the same file. See §5.6.

```
backend-practice/                 # the repo root referred to as `harness/` below
├── LICENSE                        # AGPL-3.0 (see §5.5)            [Agent A]
├── README.md                      #                                 [Agent A]
├── .gitignore                     # ignores .env, node_modules, .forge-verify, agent-a-work
├── contract/
│   └── types.ts                   # FROZEN. Copied verbatim into BOTH apps. Two agreed
│                                  # additive changes since h0 — see §6.
├── fixtures/                      # the handshake — see §5.6
│   ├── sample-generated.json      # [Agent A]  real generator output, carries `preset`
│   ├── attack-snippets.json       # [Agent B]  schema harness-attack-snippets/v1, 14 snippets
│   ├── sample-compile.json        # [Agent B]  REAL /compile output, via fixtures:refresh
│   ├── sample-audit.json          # [Agent B]  REAL /audit output in demo step 4's state
│   └── sample-simulate.json       # [Agent B]  REAL /simulate capture from the VE
│
├── harness-web/                   # ███ AGENT A ███  Next.js on Vercel
│   ├── package.json               #     its own deps, its own lockfile
│   ├── src/types.ts               #     copy of contract/types.ts
│   ├── src/generator/
│   │   ├── aave/flashLoanReceiver.ts
│   │   ├── aave/erc4626Vault.ts
│   │   ├── aave/deployScript.ts
│   │   └── attacks/{assembleAttackTests,scaffold,addresses}.ts
│   ├── src/app/                   #     pages, options panel, CodeMirror, audit UI, /api/compile
│   └── scripts/                   #     emit, fixture, preview, matrix, schema-guard, zip-check
│
└── harness-api/                   # ███ AGENT B ███  standalone Express service
    ├── package.json               #     its own deps, its own lockfile
    ├── remappings.txt             #     served at GET /remappings
    ├── src/types.ts               #     copy of contract/types.ts
    ├── src/{server,env,validate,aave,tenderly,solc-imports}.ts
    ├── src/routes/
    │   ├── compile.ts             #     solc standard-JSON, imports resolved off disk
    │   ├── audit.ts               #     rule engine (comment/string masking, CRLF+BOM safe)
    │   ├── deploy.ts              #     viem → Tenderly
    │   └── simulate.ts            #     scenario runner, entrypoint auto-detected
    ├── src/scripts/               #     create-vnet, seed-balances, smoke, compile-check,
    │                              #     route-check, chain-check, verify-generated,
    │                              #     refresh-fixtures, audit-file, selector-probe
    ├── knowledge/findings.json    #     15 curated findings + hack citations
    └── contracts/samples/         #     hardened reference contracts the rules are tuned against
```

**Verification entry points** (`npm run <name>` inside `harness-api`):
`smoke` (offline, no chain) · `compile:check` · `route:check` · `chain:check` · `verify:generated`
· `fixtures:refresh` · `vnet:create` · `vnet:seed`.

**Why two `package.json` files instead of a monorepo:** a shared lockfile is the single most common
way two AI agents deadlock — every `npm i` from one agent dirties the other's tree. Two independent
apps means two installs, two dev servers, two deploys, zero contention. The cost is one duplicated
60-line types file, which is a trade worth making at this timescale.

**Deployment:** `harness-web` → Vercel. `harness-api` → Vercel (separate project), Railway, or
Render. Agent B **must enable CORS** for the web origin.

### 5.2 Stack — all decisions are pre-made, do not re-litigate

| Layer | Pick | Why |
|---|---|---|
| Codegen | `npm i @openzeppelin/wizard` — use its exported `ContractBuilder` + `printContract` | Free OZ-grade formatting, import dedup/sorting, override handling, remappings |
| App | **Next.js App Router on Vercel** | Server routes needed for compile + Tenderly |
| Editor | **CodeMirror 6 + `@replit/codemirror-lang-solidity`** (MIT, 132k wk downloads) | ⚠️ **Monaco has NO Solidity grammar** — you would hand-roll a Monarch tokenizer. Do not use Monaco. |
| Cheap fallback | `highlight.js` → `hljs.highlight('solidity', code)` | Exactly what OZ's own UI does. Read-only preview in ~20 lines. |
| Formatting | `prettier-plugin-solidity` — **OPTIONAL** | `printContract` output is already clean, and the plugin **throws on syntactically invalid input**. Skip unless time permits. |
| Compile | **SERVER-SIDE**: Next route + npm `solc` | ⚠️ Browser solc's import callback is **synchronous** — every OZ/Aave source must be in memory before compiling; the documented workaround is a fetch-and-recompile loop. Do not attempt this at hour 9. |
| Deploy | **viem `deployContract`** onto a **pre-created** Tenderly Virtual Environment | No forge, no Docker, no CI |

### 5.3 ⭐ How to extend OZ Wizard (the single biggest time-saver)

**`@openzeppelin/wizard` v0.10.13 exports `ContractBuilder`, `printContract`, `OptionsError`, and
the `Contract`/`Options` types.** Proof: OZ's own `packages/core/confidential/src/print.ts` and
`packages/ui/src/mcp-apps/confidential/adapter.ts` import exactly these.

**OZ has already built our extension pattern twice.** `@openzeppelin/wizard-uniswap-hooks` and
`@openzeppelin/wizard-confidential` are thin packages that reuse `ContractBuilder` + `printContract`
and add only `additionalCompatibleLibraries: [{ name, path, version, alwaysKeepOzPrefix }]`.
**`core/uniswap-hooks/src/print.ts` is ~17 lines.** Copy that shape exactly.

Internals to know: it is **not** string templates and **not** an AST — it is a small in-memory IR
plus a pretty-printer. `ContractBuilder` accumulates imports / parents / functions / structs /
variables / natspec; `printContract(contract, opts)` emits SPDX → pragma → imports →
`contract X is ...` → body. `erc20.print(opts)` is literally `printContract(buildERC20(opts))`.

Also worth copying: `core/solidity/src/zip-foundry.ts` → `zipFoundry(c, opts)` builds a JSZip
containing `src/X.sol`, `test/X.t.sol`, `script/X.s.sol`, `setup.sh`, `README.md`.

❌ **Do NOT fork the OZ monorepo.** Yarn workspaces + Deno checks + changesets + a Svelte UI +
147 releases of churn ≈ 3 hours burned for zero benefit.

### 5.4 ⚠️ Security requirement (non-negotiable)
**CVE-2026-48054**: OZ Wizard `<0.10.9` interpolated `opts.name` / `opts.uri` straight into
generated Hardhat test TypeScript, enabling injection.

**Every user-controlled value we interpolate into generated Solidity, tests, scripts, or the zip
must be strictly validated and escaped.** Enforce: contract names match `^[A-Za-z_][A-Za-z0-9_]*$`,
addresses match `^0x[a-fA-F0-9]{40}$`, numerics are bounded integers. Reject, do not sanitize.

### 5.5 ⚠️ Licensing
`@openzeppelin/wizard` is **AGPL-3.0-only** (relicensed from MIT ~2 years ago). §13 is a *network*
copyleft: hosting a web app that links it obliges us to offer complete corresponding source under
AGPL. **For a hackathon this is fine** — ship a public repo with an AGPL `LICENSE` file. Flag it
before any commercialization. Generated contracts are unaffected (they carry their own SPDX header).

### 5.6 SEPARATION PROTOCOL — how the two agents avoid ever blocking each other

**Can they work completely separately? Yes — after a 30-minute joint setup, with exactly one
30-minute rejoin at h10.** Everything below exists to make that true.

#### The four things that MUST be frozen at hour 0 (do these together, then split)

| # | Artifact | Why it must be frozen |
|---|---|---|
| 1 | `contract/types.ts` (§6) | The HTTP wire format. Copy into both apps. **Never edited again** without both agents present. |
| 2 | **Finding ID vocabulary** (below) | Agent A tags `appliedFindingIds`; Agent B keys `findings.json` and `attack-snippets.json`. If A guesses `FL-1` and B writes `AAVE-FL-001`, nothing lines up and you find out at hour 10. |
| 3 | **Remappings + import paths** | A's generated Solidity does `import "@aave/core-v3/..."`; B's solc must resolve that exact string. Agree the array verbatim. |
| 4 | **API base URL + route names** | `POST {API_BASE}/compile`, `/audit`, `/deploy`, `/simulate`. A keeps `API_BASE` in one env var and mocks until h10. |

**Frozen finding IDs — use these exact strings. They map 1:1 to §8.1:**
```
AAVE-FL-001   callback not gated to Pool + initiator        critical
AAVE-FL-002   attacker-controlled calldata in params        critical
AAVE-VLT-003  aToken.balanceOf as share denominator         critical
AAVE-VLT-004  withdraw return value ignored                 high
AAVE-RISK-005 risk engine re-implemented more permissively  high
AAVE-RISK-006 LTV-0 aToken poison dust                      high
AAVE-ORC-007  eMode price-source divergence                 high
AAVE-VLT-008  RewardsController rewards unclaimable         high
AAVE-VLT-009  no ERC20 escape hatch                         medium
AAVE-RISK-010 supply/borrow cap + pause reverts unhandled   medium
AAVE-VLT-011  ERC-4626 receiver/owner conflated             medium
AAVE-ORC-012  oracle scale/decimals mismatch                high
AAVE-FL-013   idle funds held on flash-loan receiver        medium
AAVE-SWP-014  missing minAmountOut on swap leg              high
AAVE-DEP-015  unchecked external call return value          medium
```

#### The fixture handshake — this is what actually decouples them

By **hour 1**, each agent commits realistic sample payloads to `fixtures/`, then builds against the
*other agent's fixture* — never against their live service.

- **Agent A publishes** `sample-generated.json` (a real `GeneratedProject`). Agent B compiles and
  audits **that file** all day and never needs A's UI running.
- **Agent B publishes** `sample-compile.json`, `sample-audit.json`, `sample-simulate.json`. Agent A
  renders **those files** all day and never needs B's server running.
- **Agent B also publishes** `attack-snippets.json` by h4 (see §8.2): Solidity test bodies keyed by
  finding ID, using placeholder tokens `{{CONTRACT}}`, `{{POOL}}`, `{{ASSET}}`, `{{ATOKEN}}`.
  Agent A substitutes and stitches; Agent A never has to research an exploit.

If a fixture must change, the publishing agent updates it and says so. That is the only cross-agent
communication required for 10 hours.

#### What each agent can do with ZERO knowledge of the other

| Agent A, entirely alone | Agent B, entirely alone |
|---|---|
| Every generator template + `printContract` wiring | Tenderly VE creation, funding, hello-world deploy |
| Attack-test assembly + placeholder substitution | Attack-snippet authoring + `findings.json` |
| Options panel, CodeMirror preview, tabs | solc pipeline + import resolution |
| Audit results UI (from `sample-audit.json`) | Audit rule engine (against `sample-generated.json`) |
| Compile/deploy result cards (from fixtures) | Deploy + simulate against real mainnet Aave |
| Preset buttons, Remix link, zip export, README | Balance seeding, scenario scripting, CORS |

#### The one unavoidable rejoin — budget 30 minutes at h10  ✅ AS BUILT

Agent A swaps mocks for the real `API_BASE`. Expect CORS, one JSON field-name mismatch, and one
serialization bug (bigint → string). **This is the only sequential dependency in the plan. Do it at
h10, not h13.** If integration has not started by h11, ship the fixture-backed demo — it still shows
the complete flow.

> **What the rejoin actually surfaced.** The bigint bug was pre-empted (Agent B set a JSON replacer
> up front) and CORS was pre-tested with real browser semantics. The genuine problems were
> different, and none would have been caught by fixtures alone:
>
> 1. **Agent B's audit engine produced four false positives** on Agent A's contract. The `absence`
>    rules encoded *Agent B's own* identifiers rather than the concept, so they demanded oracle
>    scaling and health-factor checks from a contract that never touches an oracle and never
>    borrows. Rules are now conditional on evidence that the contract performs the risky operation:
>    `(mitigation)|^(?![\s\S]*trigger)`, compiled **without** the `m` flag so `^` anchors to the
>    whole source.
> 2. **The frozen entrypoint was wrong, and Agent B adapted rather than Agent A.** The generator
>    emits `initiateFlashLoan(uint256, FlashParams)`; Agent B had frozen
>    `executeFlashLoan(address,uint256,bytes)`. A typed struct with no `bytes` payload *is* the
>    AAVE-FL-002 mitigation — forcing the `bytes` shape would have reintroduced the finding. §`/simulate`
>    now detects the entrypoint from the deployed dispatch table and drives either shape.
> 3. **A UTF-8 BOM broke `/compile`** with a line-1 `ParserError`. Now stripped in compile, audit
>    and validation.
> 4. **The two agents wrote `attack-snippets.json` to different schemas.** Agent A's assembler won,
>    because it was the consumer and had better guardrails; Agent B ported the content into it.
>
> **Lesson for the next build:** fixtures prove the *shape* of an interface, not its *semantics*.
> Everything above needed the two halves actually talking to each other. Budget the rejoin for
> discovering that your rules are wrong, not just that your field names are.

#### Git hygiene
- One branch each: `agent-a`, `agent-b`. Merge to `main` only at h10 and h13.
- Because the two apps share **no files**, merges are near-conflict-free by construction.
- The only shared paths are `contract/`, `fixtures/`, `LICENSE`, `README.md` — each has exactly one
  owner.

---

## 6. THE SHARED INTERFACE CONTRACT — write this file FIRST, together, in the first 20 minutes

Both agents depend on these types. Agree on them before splitting up, then never change them
without telling the other agent. `packages/generator/src/types.ts`:

> **✅ AS BUILT.** The file lives at `contract/types.ts`, copied verbatim into `harness-web/src/`
> and `harness-api/src/`. It also carries the other three frozen artifacts of §5.6 as constants:
> `FINDING_IDS`, `REMAPPINGS` / `IMPORT_PATHS`, and `API_ROUTES`, plus `SOLC_VERSION` (`0.8.27`),
> `SOLIDITY_PRAGMA` (`^0.8.27`) and `EVM_VERSION` (`cancun`).
>
> **Two additive changes were agreed after h0** and applied to all three copies. Both are marked
> ✅ below. Neither removes or renames anything, so no existing consumer broke.

```ts
export type Preset = 'aave-v3-flashloan-receiver' | 'aave-v3-erc4626-vault';

export interface GenerateOptions {
  preset: Preset;
  name: string;                  // ^[A-Za-z_][A-Za-z0-9_]*$
  access: 'none' | 'ownable' | 'roles';
  pausable: boolean;
  asset?: `0x${string}`;         // underlying ERC20
  routerAllowlist: boolean;
  claimRewards: boolean;
  sweepEscapeHatch: boolean;
  depositCap?: string;           // decimal string, vault preset only
  feeBps?: number;               // 0..1000, vault preset only
}

export interface GeneratedProject {
  preset: Preset;                // ✅ ADDED post-h0. Consumers must not infer this from the source.
  contractName: string;
  contractSource: string;        // src/<Name>.sol
  attackTestSource: string;      // test/<Name>.attack.t.sol
  deployScriptSource: string;    // script/<Name>.s.sol
  remappings: string[];
  appliedFindingIds: string[];   // which knowledge/findings.json rules are mitigated
}

export interface Finding {
  id: string;                    // e.g. 'AAVE-FL-001'
  title: string;
  severity: 'critical' | 'high' | 'medium' | 'low';
  vulnClasses: string[];         // AuditVault slugs, e.g. ['vuln/access-control/missing-auth']
  summary: string;               // one sentence, plain English
  detail: string;                // 2-4 sentences
  incidents: { name: string; loss?: string; url: string; pocFolder?: string }[];
  detect: { kind: 'regex' | 'absence'; pattern: string; appliesTo: Preset[] }[];
  remediation: string;
}

export interface AuditResult {
  findings: (Finding & { status: 'mitigated' | 'triggered'; line?: number })[];
  score: { mitigated: number; triggered: number };
}

export interface CompileResult {
  ok: boolean;
  abi?: unknown[];
  bytecode?: `0x${string}`;
  sizeBytes?: number;
  errors: { severity: string; message: string; line?: number }[];
}

export interface DeployResult { address: `0x${string}`; explorerUrl: string; txHash: string; }

export type Scenario =
  | 'supply-borrow'
  | 'flashloan-simple'
  | 'leverage-loop'
  | 'vault-deposit';            // ✅ ADDED post-h0 for the vault demo. Purely additive.
export interface SimulateResult {
  ok: boolean;
  scenario: Scenario;
  trace: { depth: number; from: string; to: string; fn: string; value?: string }[];
  balanceChanges: { token: string; delta: string }[];
  explorerUrl?: string;
}
```

**Both agents mock the other side immediately** (Agent A hardcodes a fake `CompileResult`; Agent B
hardcodes a fake `GeneratedProject`) so neither ever blocks on the other.

---

## 7. WORK SPLIT

> **File ownership is absolute.** If you need a change in the other agent's files, request it —
> do not edit. This is the #1 cause of two-agent hackathon failure.

> ✅ **AS BUILT — all A1–A10 and B1–B10 are complete except the backup video (B10) and deployment.**
> Two deviations worth recording:
> **A7 (vault preset) was promoted from stretch to core** when the demo became vault-first, and
> **B9 (leverage-loop) was not cut** — it ships and passes on-chain.
> Agent A stopped work after the handoff, so Agent B completed the remaining generator change
> (recording `AAVE-RISK-006` where the escape hatch mitigates it), ran the `forge` verification,
> and merged the two trees into the §5.1 layout.

### AGENT A — Generator + Frontend
**Owns:** `harness-web/**` (entire app), plus `contract/types.ts`, `fixtures/sample-generated.json`,
`README.md`, `LICENSE`

| # | Task | Depends on | Done when |
|---|---|---|---|
| A1 | Scaffold Next.js App Router, deploy hello-world to Vercel, `npm i @openzeppelin/wizard`, add AGPL `LICENSE` | — | Public URL loads |
| A2 | Write `types.ts` (§6) **with Agent B** | — | Both agents import it |
| A3 | `buildFlashLoanReceiver(opts): ContractBuilder` + `print()`. Mimic `wizard-uniswap-hooks`. Implement every mitigation in §8.1 | A1, A2 | `print(defaults)` returns valid Solidity |
| A4 | Live preview UI: CodeMirror 6 + Solidity mode, options panel, tabs for `contract` / `attack tests` / `deploy script` | A3 | Toggling a checkbox re-renders instantly |
| A5 | **Attack-test assembler** — read `fixtures/attack-snippets.json`, substitute `{{CONTRACT}}` / `{{POOL}}` / `{{ASSET}}` / `{{ATOKEN}}`, emit `test/<Name>.attack.t.sol` with the right imports and setUp. Only include snippets whose finding ID is in `appliedFindingIds` (§8.2) | A3, B3b | ≥6 tests emitted, file reads convincingly |
| A6 | Audit results UI — findings list, severity chips, mitigated ✅ vs triggered ❌, expandable detail with the historical hack + link to its PoC folder | A2 (mock B's response) | Renders from a static `AuditResult` fixture |
| A7 | `buildErc4626Vault(opts)` + its attack tests | A3, A5 | Second preset selectable |
| A8 | Preset buttons + result cards + error states. **Never type into a form on stage** | A4 | 2 one-click presets |
| A9 | "Open in Remix" button (§9.3) + `.zip` download via JSZip | A3 | Remix opens with our code |
| A10 | README, landing copy, submission text | all | Repo is public and self-explanatory |

### AGENT B — Knowledge Base + Attack Content + Chain Infrastructure
**Owns:** `harness-api/**` (entire service, including `knowledge/` and `contracts/`), plus
`fixtures/attack-snippets.json`, `fixtures/sample-compile.json`, `fixtures/sample-audit.json`,
`fixtures/sample-simulate.json`

| # | Task | Depends on | Done when |
|---|---|---|---|
| B1 | Create the **Tenderly Virtual Environment** via REST (§9.2). Store Admin + Public RPC in env vars, fund a key, hand-deploy a hello-world | — | A tx is visible on the public explorer |
| B2 | Write `types.ts` (§6) **with Agent A** | — | Both agents import it |
| B3 | **`knowledge/findings.json`** — curate ≥12 findings from §8.1 into the `Finding` shape, each with AuditVault `vulnClasses`, real incidents, URLs, and `pocFolder` references from §3.4 | B2 | Valid JSON, ≥12 entries, ≥4 critical |
| B3b | **`fixtures/attack-snippets.json`** — the Solidity body of each attack test from §8.2, keyed by finding ID, using placeholder tokens `{{CONTRACT}}` / `{{POOL}}` / `{{ASSET}}` / `{{ATOKEN}}`. Each carries a header comment citing its real incident + URL. **Publish by h4 — Agent A is blocked on this for A5.** | B3 | ≥6 snippets, each valid Solidity once substituted |
| B4 | `/api/compile` — solc standard-JSON, OZ + Aave sources resolved from `node_modules`, returns `CompileResult` | B2 | Green ✅ + ABI + bytecode size |
| B5 | `/api/audit` — rule engine running `Finding.detect` (regex + absence checks) over submitted source, returns `AuditResult`. **Must work on user-EDITED code, not just generated code** | B3 | Deleting a `require` from generated code flips a finding to ❌ |
| B6 | `/api/deploy` — viem `deployContract` onto the Tenderly VE, returns address + public explorer URL | B1, B4 | Shareable verified-source URL |
| B7 | `/api/simulate` — run `supply-borrow` and `flashloan-simple` against **real mainnet Aave** on the VE; return call trace + balance changes | B6 | Trace JSON renders |
| B8 | Seed the VE: `tenderly_setBalance` + `tenderly_setErc20Balance` so the demo account holds real USDC/WETH | B1 | Balances visible on explorer |
| B9 | `leverage-loop` scenario (supply → borrow → swap → supply again) | B7 | Third scenario runs |
| B10 | End-to-end dry run ×3; **record the backup video at h12** | all | Video file exists |

### ⚖️ Balance note — READ THIS

After moving attack-test *authoring* to Agent B (task B3b) and leaving Agent A only the *assembly*,
the rough effort estimate is:

| | Tasks | Est. effort | Hard core | Stretch (cuttable) |
|---|---:|---:|---|---|
| **Agent A** | 10 | ~11.5h | A5 assembler, A4 preview UI | A7 vault preset, A9 Remix/zip |
| **Agent B** | 11 | ~12.5h | B7 simulation, B3b attack snippets | B9 leverage-loop |

**Agent B carries roughly 1 hour more.** That is deliberate and correct: the attack snippets belong
with `findings.json` (same research, same citations, same IDs), and splitting them would have forced
Agent A to independently research exploits — duplicated work and a guaranteed ID mismatch.

**To compensate, Agent A absorbs:** the h10 integration work on the web side, the README, the
landing copy, and the submission text. If Agent B is running behind at h9, **cut B9 (leverage-loop)
first** — it is pure bonus.

**The one hard hand-off:** B3b must be published by **h4** or Agent A stalls on A5. If B is late,
Agent A writes two placeholder snippets to unblock, and B overwrites them later.

---

## 8. Content the generator must encode

### 8.1 The audit corpus — 15 verified findings
These are real, sourced, recurring integration bugs. Agent A hardens against them; Agent B turns
them into `findings.json`.

| # | Bug | Sev | Real incident |
|---|---|---|---|
| 1 | **Flash-loan callback treated as private.** Only checks `msg.sender == Pool`. **Anyone** can call `Pool.flashLoan` naming *your* contract as receiver, so Aave invokes your callback with attacker-controlled `params` | **CRIT** | DODO MarginTrading (Sherlock #150); Mimo SuperVault (C4 H-02) |
| 2 | Attacker-supplied calldata inside the callback (`approve(target, max)` + `target.call(data)`) turns #1 from a revert into a drain | **CRIT** | Mimo `aggregatorSwap(dexTxData)` |
| 3 | **`aToken.balanceOf(this)` used as share-price denominator** → first-depositor / donation inflation. aTokens are freely ERC20-transferable, so donation needs **no** protocol interaction | **CRIT** | PoolTogether AaveV3YieldSource (C4 H-01) |
| 4 | Using the *requested* amount instead of the *returned* amount on withdraw — Aave's `withdraw` returns the actual amount; the pool may be capped, frozen, paused or illiquid | High | Connext Amarok (C4 M-15) |
| 5 | Re-implementing Aave's risk engine **more permissively** than Aave (eMode, isolation, siloed, debt ceilings, LTV-0, pause/freeze, config bitmap) | High | Spearbit Morpho-Aave v3 |
| 6 | **LTV-0 aToken poison dusting** — 1 wei of a zero-LTV aToken can block withdrawals | High | StErMi Aave bug bounty; ChainSecurity Grove ALM |
| 7 | eMode price-source divergence (`priceSource == address(0)` fallback semantics) | High | Spearbit |
| 8 | Aave base yield ≠ incentives. `RewardsController` rewards are permanently lost without a claim path — and you must pass **aTokens**, not underlyings | Med/High | Float Capital (C4 M-05); Alchemix V3 Immunefi #57812 / #58080 |
| 9 | No ERC20 escape hatch → Merkl rewards and airdrops stuck forever | Med | Morpho integration checklist |
| 10 | Supply/borrow caps + pause/freeze cause unexpected reverts; leverage loops brick | Med | Sherlock Index #267 |
| 11 | ERC-4626 `receiver` vs `owner` mixed up — burn Alice's shares, pay Bob | Med | Taichi Pt.5 |
| 12 | Oracle scale / decimals mismatch silently mis-sizes borrows | High | Morpho liquidation docs |
| 13 | **Idle funds held on a FlashLoanReceiverBase** → an attacker's flash loan is repaid out of your balance | Med | Aave docs warn explicitly; ESE 92391 |
| 14 | Missing slippage / `minAmountOut` on the swap leg | High | `vuln/defi/slippage` — 120 registry tags |
| 15 | Unchecked external call return values | Med | `vuln/dependency/unchecked-return-value` |

**Bonus footgun worth encoding:** the multi-asset `flashLoan` with `interestRateModes = 2` **opens
debt** instead of repaying, and `onBehalfOf` must have pre-approved borrow allowance.
`flashLoanSimple` avoids this entirely — **default to `flashLoanSimple`.**

### 8.2 Required attack tests (Agent A, task A5)  ✅ AS BUILT — 7 per preset, 14/14 passing

`fixtures/attack-snippets.json` is schema **`harness-attack-snippets/v1`**, owned by Agent B and
consumed by Agent A's assembler. Keys are finding IDs, optionally `<FINDING_ID>#<variant>` when a
finding needs a different body per preset. Each snippet carries `testName`, `title`, `presets`,
`incidents`, `comments`, `body` (statements placed inside the test) and optional `helpers`
(contract-level). Placeholders: `{{CONTRACT}}` `{{CONTRACT_TYPE}}` `{{POOL}}` `{{ASSET}}`
`{{ATOKEN}}` `{{PARAMS}}` `{{PARAMS_STRUCT}}` `{{OWNER}}`.

A snippet is emitted only when its finding ID is in `appliedFindingIds` **and** its preset matches.
Two guardrails, both of which caught real bugs: the assembler throws if it would emit **zero**
tests (a schema mismatch otherwise silently produces an empty suite that looks like success), and
throws on any surviving `{{PLACEHOLDER}}`. Agent B's `smoke` additionally rejects two snippets
declaring the same helper, since helpers are emitted per snippet without de-duplication.

**Flash-loan receiver — 7 tests, all passing:**

1. `test_RejectsThirdPartyInitiator` — a random EOA names our contract as receiver → reverts *(DODO, Mimo)*
2. `test_RejectsAttackerControlledParams` — hostile `params` → reverts *(Mimo)*
3. `test_IdleFundsCannotRepayAttackerLoan` — an idle balance survives a third-party loan attempt *(Aave docs)*
4. `test_RejectsZeroMinAmountOut` — no slippage floor → reverts *(`vuln/defi/slippage`)*
5. `test_PausedInitiationRevertsCleanly` — halt is clean and reversible *(Sherlock Index)*
6. `test_SweepIsGatedAndRecoversStrayTokens` — escape hatch is owner-only *(Morpho checklist)*
7. `test_SurvivesZeroLtvDust` — 1 wei of a zero-LTV aToken stays inert *(StErMi)*

**ERC-4626 vault — 7 tests, all passing:**

1. `test_ResistsATokenDonation` — **the demo test**; donation must not move share price *(PoolTogether)*
2. `test_WithdrawAccountsActualAmount` — short fill is not booked as full *(Connext)*
3. `test_ReceiverOwnerNotConflated` — burn owner's shares, pay receiver *(Taichi Pt.5)*
4. `test_SweepCannotTouchPrincipal` — hatch refuses asset and aToken *(Morpho checklist)*
5. `test_RewardsAreClaimableAndGated` — incentives claimable, claim is authorised *(Float Capital)*
6. `test_PausedDepositRevertsCleanly` — no partial state left behind *(Sherlock Index)*
7. `test_SurvivesZeroLtvDust` — dust does not brick redemptions *(StErMi)*

> ⚠️ **`test_RevertsWhenSupplyCapPinnedToZero` was not built as specified.** Aave's supply cap is
> enforced inside the Pool against live utilisation, which cannot be forced on a fork and is not
> mockable through `getConfiguration`. The pause path (`test_Paused…RevertsCleanly`) asserts the
> same property — a capped/halted reserve produces a clean revert with no bricked state — through a
> mechanism that is actually reachable.

### 8.3 ⚠️ Ethical constraint
**Never emit deliberately vulnerable code.** No "toggle to introduce a bug." Generated output is
always hardened. For demo drama, ship a read-only *"What this test catches"* panel showing the naive
tutorial pattern beside the test that kills it. Same impact, defensible optics.

---

## 9. Verified technical reference

### 9.1 Minimal correct Aave v3 surface
- **Discovery:** `PoolAddressesProvider.getPool()` / `.getPriceOracle()` — **never hardcode Pool
  addresses**; markets are chain- and provider-specific.
- `supply(asset, amount, onBehalfOf, referralCode)` — approve the **underlying** first;
  `referralCode = 0` (program inactive).
- `withdraw(asset, amount, to) returns (uint256)` — **use the return value**; `type(uint256).max`
  means "everything".
- `borrow(asset, amount, interestRateMode, referralCode, onBehalfOf)` — `interestRateMode` is
  **always 2** (stable is deprecated). `onBehalfOf != msg.sender` requires `approveDelegation` on
  the variableDebtToken.
- `repay(asset, amount, 2, onBehalfOf)` — `type(uint256).max` **only** when repaying your own debt.
- **Reads:** `getUserAccountData(user)` → `(totalCollateralBase, totalDebtBase,
  availableBorrowsBase, currentLiquidationThreshold, ltv, healthFactor)`; `getReserveData`;
  `getConfiguration`; `getReserveAToken`; `getUserEMode`; `FLASHLOAN_PREMIUM_TOTAL`.
- **Flash loan:** prefer `flashLoanSimple(receiver, asset, amount, params, 0)`. The receiver
  implements `IFlashLoanSimpleReceiver.executeOperation` and **approves the Pool for
  `amount + premium` — Aave PULLS, you do not push.** Premium initialized at 0.05%,
  governance-adjustable.
- **L2:** `L2Pool` takes a packed `bytes32 args` built via `L2Encoder`; `flashLoan` and
  `setUserEMode` are **not** compressed and stay on the normal Pool. *(Out of scope for v1.)*
- ⚠️ Aave v4 docs are live (Hub & Spoke, Target-HF liquidations) but **mainnet deployment is
  unverified — target v3.**

#### ✅ Learned on a real mainnet fork (not in the original research)

- **Aave v3.3 uses custom errors, not numeric string codes.** `InvalidAmount()` is `0x2c5211c6`.
  viem cannot decode them unless you pass the contract's own ABI, so a revert surfaces as
  *"signature not found on ABI"*. Identify unknown selectors by computing candidates —
  `harness-api/src/scripts/selector-probe.ts` does this.
- **A 1 wei supply reverts.** Aave divides the amount by the reserve's liquidity index (>1e27); a
  scaled result of zero is rejected. Any dust position needs roughly `10^(decimals-3)`, not 1.
- **aToken balances are scaled.** Never forge them with `deal` or `tenderly_setErc20Balance` — the
  written slot does not match the scaled representation. Acquire them by actually supplying.
- **Zero-LTV collateral only matters while borrowing.** `validateHFAndLtv` runs from
  `executeWithdraw` only when the account has debt, so a supply-only integration is unaffected by
  LTV-0 dust; the practical mitigation is an escape hatch that can move the dust out.
- **USDC on Ethereum v3 is `ltv 7500`, active, unfrozen**, `supplyCap` 2.5B, `borrowCap` 2.25B —
  verified at block 25710954, contrary to an assumption that stablecoin LTV had been zeroed.
- **Tracers differ in shape.** `tenderly_traceTransaction` returns a FLAT array under `.trace`
  where depth is `traceAddress.length`; `debug_traceTransaction` + `callTracer` returns a NESTED
  tree under `.calls`. Assuming one shape silently yields a 1-entry trace that looks like it worked.
- **Impersonation needs no key.** `eth_sendTransaction` with any `from` works on a Virtual
  Environment, which is how the vault scenario drives a separate attacker and victim.

### 9.2 Tenderly Virtual Environments
Base URL: `https://api.tenderly.co/api/public/v1/account/{account}/project/{project}`

- `POST /environments` with:
  `network_configs: [{ network_id: "1", block_number, chain_config_overrides.chain_id,
  explorer_config: { enabled: true, contract_verification_visibility: "src" },
  accounts: [{ address, balance }] }]`
  → response `active_instance.vnets[].rpcs` gives **Admin RPC** and **Public RPC**.

  > ✅ **CORRECTED.** This spec originally said
  > `explorer_config: { verification_visibility: "source" }`. Tenderly rejects that with
  > `400 explorer_config.contract_verification_visibility is invalid` — note the error names the
  > field it validates, not the field you sent. The working shape is
  > **`contract_verification_visibility: "src"`**. `create-vnet.ts` probes candidate shapes and
  > reports which one was accepted, so a future API change surfaces immediately.
- Admin-RPC cheatcodes: `tenderly_setBalance`, `tenderly_setErc20Balance`,
  `tenderly_setStorageAt`, `evm_snapshot` / `evm_revert`, `evm_increaseTime`,
  `eth_sendTransaction` (impersonation).
- Simulate:
  `POST /environments/{env}/instance/{inst}/vnet/{vnet}/transactions/simulate` — supports block and
  state overrides; returns **call trace, state changes, emitted events**.
- It forks **mainnet**, so you get real Aave pools, real liquidity, real oracle prices. Sepolia
  cannot do this.
- **The verified-source public explorer page is our demo artifact.**
- ⚠️ **Create the VE ahead of time and hardcode the RPC. Never create one live on stage.**
- Skip `tenderly/vnet-github-action` — it is a repo-CI story and does not demo.

### 9.3 "Open in Remix" (exact mechanism, from OZ's `ui/src/solidity/remix.ts`)
```
https://remix.ethereum.org/#code=<b64>
b64 = btoa(String.fromCharCode(...new TextEncoder().encode(source))).replace(/=*$/, '')
```
Optional extras: `&remaps=<b64 of remappings.join('\n')>` ⚠️ *(not in Remix's public doc list, but
OZ ships it in production)*, `&activate=solidity,solidityUnitTesting`, `&autoCompile=true`,
`&version=soljson-v0.8.x+commit.hash`. ~30 minutes to implement; gives compile **and** in-browser
unit testing with zero infrastructure.

---

## 10. Timeline (14h)

| Hours | Agent A | Agent B |
|---|---|---|
| 0–0.5 | **Together:** freeze all four items in §5.6 — `types.ts`, finding IDs, remappings, API base URL. Then split and do not sync again until h10. | ← |
| 0.5–1 | A1 scaffold + Vercel + AGPL; **publish `sample-generated.json`** | B1 Tenderly VE + hello-world deploy; **publish the three sample-*.json fixtures** |
| 1–4 | A3 flash-loan generator, A4 preview UI | B3 `findings.json`, **B3b `attack-snippets.json` — due h4, A is blocked on this** |
| 4–8 | **A5 attack-test assembler**, A6 audit UI | B4 `/api/compile`, B5 `/api/audit`, **B6 `/api/deploy`** |
| 8–10 | A7 vault preset, A8 preset buttons | B7 `/api/simulate`, B8 seed balances |
| **10–10.5** | **Together: THE ONLY REJOIN.** A swaps mocks for the real `API_BASE`. Expect CORS + one field-name mismatch + one bigint serialization bug. | ← |
| 10.5–12.5 | A9 Remix + zip, A10 README + submission text | B9 leverage-loop scenario *(cut first if behind)* |
| 12.5–14 | **Together:** dry runs ×3, **record backup video**, submit | ← |

**Cut list, in order:** ~~leverage-loop scenario → zip download → vault preset → Remix button →
audit on user-edited code.~~

> ✅ **SUPERSEDED — nothing was cut, and the vault preset must never have been on this list.**
> Once the demo became vault-first, `A7 (vault preset)` became core and `B9 (leverage-loop)`
> stopped being the obvious first cut — both shipped and both are verified. If you ever do need to
> cut under time pressure, the honest order is now:
> **leverage-loop scenario → Remix button → zip download → flash-loan preset.**
> The vault preset, the audit engine and the attack suite are the product.

**NEVER cut:** compile ✅ · deployed Tenderly explorer link · the generated attack-test file.
Those three *are* the pitch.

---

## 11. Demo script (3 minutes)  ✅ AS BUILT — VAULT-FIRST

> The original flash-loan script is preserved below the vault one; both work. The vault leads
> because its Critical is a single deleted function and the attack needs no protocol interaction
> at all — an aToken donation is one plain ERC20 transfer.

1. **(20s)** "Here's an ERC-4626 vault over Aave. Every tutorial version has a Critical: aTokens
   are freely transferable, so anyone can donate into your vault and inflate the share price. It
   costs the attacker one ERC20 transfer and no interaction with Aave whatsoever."
2. **(30s)** Click preset **"Aave V3 ERC-4626 Vault."** Code appears. Point at `_decimalsOffset()`
   — the one-line virtual-share defence — and at `totalAssets()`.
3. **(40s)** Click **"Generate attack suite."** Seven named tests appear, each citing a real
   incident. Run → all green, against real mainnet Aave on a fork.
4. **(30s)** Delete `_decimalsOffset()`. Click **Audit.** `AAVE-VLT-003` flips to **❌ Critical**,
   citing PoolTogether's AaveV3YieldSource H-01 and linking two runnable registry PoCs. Put it
   back → ✅.
5. **(40s)** **"But does it actually work?"** → Compile ✅ → Deploy → **Tenderly public explorer
   URL, verified source** → Simulate `vault-deposit` against **mainnet Aave**. The attacker
   donates 100,000 USDC of aTokens at the vault, a victim then deposits 25,000 — and gets
   24,999.92 back. **The attacker spent 1,255,051 units for every 1 unit extracted.**
6. **(20s)** "Aave Kit and the Morpho SDK own the frontend. Nobody generates the *contract* — which
   is where every Critical in the public audit corpus actually lives. The checklist is mechanical.
   We ship it."

Step 4 is the moment that proves this is an audit tool and not a form. **Rehearse it.**

<details>
<summary>Original flash-loan script (still fully working)</summary>

1. **(20s)** "Here's a hand-written Aave flash-loan receiver from a popular tutorial. It has a
   Critical bug that drained DODO and Mimo: the callback is public. Anyone can call
   `Pool.flashLoan` naming *your* contract as the receiver."
2. **(30s)** Click preset **"Aave V3 Flash Loan Receiver."** Code appears. Point at the three gates.
3. **(40s)** Click **"Generate attack suite."** Seven named tests appear, each citing a real incident.
   Run → all green.
4. **(30s)** Delete the `initiator != address(this)` gate. Click **Audit.** `AAVE-FL-001` flips to
   ❌ — and so does `AAVE-FL-013`, because that gate is exactly what stops an idle balance being
   used to repay someone else's loan. Put it back → ✅.
5. **(40s)** Compile ✅ → Deploy → explorer URL → Simulate `flashloan-simple` against mainnet Aave.
   The initiator's balance moves by **exactly −12.5 USDC** — the 5bp premium on 25,000 — proving
   the receiver held no idle funds.

</details>

---

## 12. Objections to pre-empt

| Objection | Answer |
|---|---|
| "It's just a form over templates" | The contract is the easy half. The generated hostile fork-test suite plus the audit engine encode 15 documented findings and ~845 catalogued exploits. |
| "Why not AI-generate it?" | Determinism is the feature. OZ Wizard is deliberately not AI — that is why its output is trusted. Ours compiles 100% of the time; an LLM's does not. |
| "Aave already ships aave-vault" | Hand-written, single-purpose, not configurable, not generated, and it ships no attack suite. |
| "Doesn't OpenZeppelin already do this?" | OZ's Solidity kinds are erc20/721/1155/stablecoin/RWA/account/governor/custom. **No 4626, no DeFi, no vault.** Their *Stellar* wizard has a `vault` kind; Solidity does not. |
| "Is the generated code audit-grade?" | We never claim that. We claim it starts from the hardened pattern rather than the tutorial pattern, and ships the tests that prove it. |
| "Where does the hack data come from?" | The `evm-hack-registry` — ~845 offline-runnable exploit PoCs tagged with the AuditVault taxonomy. Every finding cites a real incident you can execute yourself. |

---

## 13. Definition of done  ✅ AS BUILT

- [ ] Public repo with AGPL `LICENSE` and a real README — *`LICENSE` and `README.md` exist; the repo is not published yet*
- [ ] Live Vercel URL — **outstanding, see §14**
- [x] Two one-click presets generating valid Solidity — both compile via `forge build` and via `/compile`
- [x] Generated `*.attack.t.sol` with ≥6 cited attack tests — **7 per preset, 14/14 passing on a mainnet fork**
- [x] `/api/audit` flips findings on user-edited code — verified for both presets
- [x] `/api/compile` returns green with ABI + bytecode size
- [x] Contract deployed to a Tenderly VE with a shareable verified-source explorer URL
- [x] ≥1 scenario simulated against real mainnet Aave, trace rendered — **4 scenarios, 25-call decoded trace**
- [ ] Backup demo video recorded — **outstanding**
- [x] Attribution to `sanbir/evm-hack-registry`, `SunWeb3Sec/DeFiHackLabs`, AuditVault, OpenZeppelin

---

## 14. What is left

1. **Deploy.** `harness-api` is a persistent Express server — Render or Railway, **≥1 GB RAM**
   (solc-js compiling OpenZeppelin + Aave will OOM on 512 MB). `harness-web` → Vercel with
   `NEXT_PUBLIC_API_BASE` pointing at the API, and the API's `WEB_ORIGIN` set to the exact Vercel
   origin. Never expose the Admin RPC to the browser; it stays server-side.
   Beware cold starts on a sleeping free instance — wake it before presenting.
2. **Record the backup video** (§13), ideally against the deployed URLs.
3. **Optional:** delete `agent-a-work/`, which now holds only Agent A's original `.git` history.

### Resilience worth knowing before you present

`harness-web` falls back to a local `/api/compile` route and a mock audit when
`NEXT_PUBLIC_API_BASE` is unset or the API is unreachable. Generate and compile keep working;
only deploy and simulate are lost. The demo degrades rather than dies.

